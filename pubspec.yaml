name: osm_path_tracker
description: "A Flutter package for live tracking and path navigation using OpenStreetMap (OSM) tiles."
version: 0.0.3
homepage: https://github.com/Brijesh-gangwar/osm_path_tracker
repository : https://github.com/Brijesh-gangwar/osm_path_tracker
environment:
  sdk: ^3.7.2
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_map: ^8.1.1
  geolocator: ^14.0.1
  latlong2: ^0.9.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0


flutter: